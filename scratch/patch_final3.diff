--- scripts/ui/character_creation/pf_character_creation_ui.gd
+++ scripts/ui/character_creation/pf_character_creation_ui.gd
@@ -34,6 +34,9 @@
	_build_ui()
	_populate_dropdowns()
	_update_ui_state()
+	_rebuild_abilities()
+	_rebuild_languages()
+	_rebuild_skills()
	line_name.grab_focus()

 func _build_ui():
@@ -107,6 +110,7 @@
	chk_alt_boosts = CheckBox.new()
	chk_alt_boosts.mouse_default_cursor_shape = Control.CURSOR_POINTING_HAND
	chk_alt_boosts.text = "Use Alternate Ancestry Boosts (2 Free)"
+	chk_alt_boosts.tooltip_text = "Replaces your ancestry's default ability boosts and flaws with two free boosts."
	chk_alt_boosts.toggled.connect(_on_alt_boosts_toggled)
	form_layout.add_child(chk_alt_boosts)

@@ -585,6 +589,13 @@
		for i in range(num_languages):
			var opt = _create_stat_dropdown(available_langs)
			languages_container.add_child(opt)
			language_opts.append(opt)
+	else:
+		var empty_state = Button.new()
+		empty_state.text = "No bonus languages available"
+		empty_state.disabled = true
+		empty_state.mouse_default_cursor_shape = Control.CURSOR_ARROW
+		empty_state.tooltip_text = "Increase your Intelligence modifier to gain bonus languages."
+		languages_container.add_child(empty_state)

 func _rebuild_skills():
	for child in skills_container.get_children():
		child.queue_free()
	skill_opts.clear()

-	if manager.draft_class_id == "": return
+	if manager.draft_class_id == "":
+		var empty_state = Button.new()
+		empty_state.text = "No skills available"
+		empty_state.disabled = true
+		empty_state.mouse_default_cursor_shape = Control.CURSOR_ARROW
+		empty_state.tooltip_text = "Select a Class first to choose your trained skills."
+		skills_container.add_child(empty_state)
+		return

	var c = _get_cached_item(_classes, manager.draft_class_id)
	var trained_count = c.get("trained_skills_count", 0) + int_mod
